/**
 * Single source of truth for the active theme package name.
 * Imported by `eleventy.config.mjs` and by tests so they can never drift.
 */
export const THEME_NAME = '@eleventy-plugin-themer/theme-base';
